# Latency benchmark configuration for STREAMING mode
# Use this config to profile continuous real-time stream processing latency
# (includes ring buffer management overhead)
#
# For single-frame discrete latency, use latency.yaml instead

defaults:
  - latency  # Inherit from base latency config

# Override engine mode to STREAMING
engine:
  mode: streaming

# Benchmark name
name: latency_streaming
