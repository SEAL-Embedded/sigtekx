# @package _global_
defaults:
  - override /engine: throughput

name: batch_scaling
description: Sweep over different batch sizes

hydra:
  mode: MULTIRUN
  sweeper:
    params:
      engine.batch: 1,2,4,8,16,32,64,128