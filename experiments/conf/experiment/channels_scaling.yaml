# @package _global_
defaults:
  - override /engine: throughput

name: channels_scaling
description: Sweep over different channel counts

hydra:
  mode: MULTIRUN
  sweeper:
    params:
      engine.channels: 1,2,4,8,16,32,64,128