# @package _global_
defaults:
  - override /engine: profiling

experiment:
  name: profiling
  description: Lightweight configuration for GPU profiling
  tags: [profiling, development, quick]
  experiment_group: profiling
