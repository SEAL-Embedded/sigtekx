# @package _global_
defaults:
  - override /engine: throughput

name: nfft_scaling
description: Sweep over different NFFT sizes

hydra:
  mode: MULTIRUN
  sweeper:
    params:
      engine.nfft: 256,512,1024,2048,4096,8192