# setup.py
from setuptools import setup

# By creating this file, we make the package structure explicit to setuptools.
# The main configuration still lives in `pyproject.toml`, but this script
# can help resolve discovery issues during editable installs and testing.

if __name__ == "__main__":
    setup()

